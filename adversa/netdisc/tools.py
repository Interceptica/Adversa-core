"""Deprecated: Python tool wrappers for network discovery.

This module has been superseded by the DeepAgent + ``ScopedBashTool`` approach
in ``adversa.netdisc.controller``.  The old ``SubfinderWrapper``,
``HttpFingerprinter``, ``TLSInspector``, and ``NmapWrapper`` classes are no
longer used and have been removed.
"""
